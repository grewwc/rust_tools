{
  "name": "knowledge_cache_manage",
  "description": "Manage knowledge cache: view stats, clear volatile cache, or force refresh.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Action to run",
        "enum": [
          "stats",
          "clear_volatile",
          "refresh"
        ]
      },
      "topic": {
        "type": "string",
        "description": "Topic to refresh (required for refresh action)."
      },
      "category": {
        "type": "string",
        "description": "Category for filtering."
      },
      "query": {
        "type": "string",
        "description": "Search query for refresh."
      },
      "limit": {
        "type": "integer",
        "description": "Max number of knowledge entries to refresh (optional; only used by the refresh action). Defaults to 10 when omitted."
      }
    },
    "required": [
      "action"
    ]
  }
}
