{
  "name": "reap_process",
  "description": "Collect a terminated child or descendant process and remove it from the process table.",
  "parameters": {
    "type": "object",
    "properties": {
      "pid": {
        "type": "integer",
        "description": "Terminated child or descendant PID."
      }
    },
    "required": [
      "pid"
    ]
  }
}
