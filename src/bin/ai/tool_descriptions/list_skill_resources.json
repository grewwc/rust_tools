{
  "name": "list_skill_resources",
  "description": "List bundled resource files for an installed skill package (references/examples/scripts). Use proactively after `load_skill` to discover what reference material is available.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Skill name as shown in `list_skills`."
      },
      "category": {
        "type": "string",
        "enum": [
          "references",
          "examples",
          "scripts"
        ],
        "description": "Optional resource category. Omit to list all resources."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 200,
        "description": "Maximum files to list (default: 80)."
      }
    },
    "required": [
      "name"
    ]
  },
  "groups": [
    "builtin",
    "skills"
  ]
}
