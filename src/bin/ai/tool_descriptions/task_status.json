{
  "name": "task_status",
  "description": "Show status of all asynchronously spawned tasks: task_id, agent, model, and current state (running/completed/failed), without blocking. Finished tasks' output is included inline and collected immediately, so you can use results without calling task_wait.",
  "parameters": {
    "type": "object",
    "properties": {}
  }
}
