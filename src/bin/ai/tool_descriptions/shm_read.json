{
  "name": "shm_read",
  "description": "Read a shared memory region by key. Returns degraded data with a warning if the owner terminated or data is corrupted. Fails only if the key is missing or permission denied, with no fallback.",
  "parameters": {
    "type": "object",
    "properties": {
      "key": {
        "type": "string",
        "description": "Key of the shared memory region to read."
      }
    },
    "required": [
      "key"
    ]
  }
}
