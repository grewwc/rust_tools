{
  "name": "search_overflow",
  "description": "Search session content moved out of context. Returns verbatim excerpts with file paths and line numbers; use read_file only when more context is needed.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search term (plain substring by default; is_regex=true for regex). Searches the current session's overflow archive (content moved out of context during compression)."
      },
      "scope": {
        "type": "string",
        "enum": [
          "all",
          "history",
          "tool_outputs"
        ],
        "description": "Where to search (default: all). 'history' = overflow-history.md (folded messages); 'tool_outputs' = tool-overflow-compressed/ (full snapshots of tool results)."
      },
      "file_pattern": {
        "type": "string",
        "description": "Optional glob to restrict searched archive files (e.g. '*execute_command*'). Applies to the tool_outputs scope."
      },
      "is_regex": {
        "type": "boolean",
        "description": "Treat query as a regex (default: false, literal substring)."
      },
      "case_sensitive": {
        "type": "boolean",
        "description": "Case-sensitive match (default: true)."
      },
      "context_lines": {
        "type": "integer",
        "description": "Context lines around each match (default: 2, max: 5)."
      },
      "max_results": {
        "type": "integer",
        "description": "Max matching lines across all files (default: 50, max: 200). If truncated, narrow the query (terms, scope, file_pattern) or reduce context_lines/max_results."
      }
    },
    "required": [
      "query"
    ]
  }
}
