{
  "name": "knowledge_consolidate",
  "description": "Two-phase consolidation: call with action=\"read_all\" to fetch all entries, then action=\"execute\" with delete_ids and/or save_entries to apply the plan. Lossless: read_all previews are truncated, so when merging entries ALWAYS list the sources in save_entries[].source_ids — the tool appends each source's full original text to the merged entry and deletes those sources automatically. Only ids in delete_ids (or auto-added from source_ids) are ever removed.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "read_all",
          "execute"
        ],
        "description": "\"read_all\" returns every entry for analysis; \"execute\" applies deletions and saves."
      },
      "delete_ids": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "List of entry IDs to delete (only used when action=\"execute\")."
      },
      "save_entries": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "content": {
              "type": "string",
              "description": "The consolidated note content."
            },
            "source_ids": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "IDs of existing entries being consolidated into this new entry. The tool appends the FULL original content of each listed source to the saved note and automatically deletes those source entries (replaced by this merged entry). Always list source_ids when merging existing entries so no content is lost."
            },
            "category": {
              "type": "string",
              "description": "Category (default: user_memory)."
            },
            "tags": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional tags for categorization."
            },
            "source": {
              "type": "string",
              "description": "Optional source context recorded with the entry (e.g. user command, project name)."
            },
            "priority": {
              "type": "integer",
              "description": "0-255. 255=permanent."
            }
          },
          "required": [
            "content"
          ]
        },
        "description": "New entries to save (only used when action=\"execute\"). Saved after deletions."
      }
    },
    "required": [
      "action"
    ]
  },
  "groups": [
    "builtin",
    "knowledge"
  ]
}
