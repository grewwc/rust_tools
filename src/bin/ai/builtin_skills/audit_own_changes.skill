---
name: audit_own_changes
version: 1.0.0
description: 在 main agent 完成某个用户需求的改动后使用。根据自己本次的改动生成审计指令，通过审计命令（/audit -f）启动审计 subagent，只审计 main agent 自己做的改动、不审计其他人的改动；随后 main agent 接管并 double check 审计结果，据此做下一步改动。
author: rust_tools
priority: 0
---
# 自查审计：只审计 main agent 自己的改动

## 触发时机

当你（main agent）完成某个用户需求的代码/文件改动，改动已落盘、自测通过、准备向用户汇报完成之前，激活本 skill 做一轮自查审计。审计只针对你本次为这个需求做的改动。

## 步骤

### 1. 生成审计指令 xxxx（自包含、边界明确）

基于你自己本次为这个需求做的改动，生成一段完整的审计指令 xxxx，必须包含以下内容：

- **审计边界（最重要）**：在 xxxx 开头明确声明"只审计 main agent（我）本次为这个需求做的改动"，并列出本次改动的文件清单。明确要求审计 subagent **不要审计、不要报告** 工作区中其他人或其他并行任务的改动、历史遗留问题——除非它们与本次改动直接相关并影响本次改动的正确性。
- **改动说明**：逐文件说明改了什么、为什么改，让 subagent 能把工作区 diff 对应到你的改动意图，从而区分"你的改动"与"别人的改动"。
- **重点验证项**：列出你希望 subagent 逐一验证的关键不变式、边界条件、回归风险点（例如：类型/编译是否通过、行为变更点、错误处理、并发/副作用、配置文件变更的影响面）。

注意：审计 subagent 完全看不到你的对话历史，它只能看到 xxxx 以及本会话的文件改动上下文，所以 xxxx 必须自包含、可独立执行。

### 2. 触发审计 subagent

通过审计命令启动审计 subagent，把 xxxx 作为审计指令传入：

- 调用 `/audit -f xxxx`（`-f` 即 fast 模式）启动审计 subagent。
- 该命令会自动附带本会话 main agent 已做的文件改动上下文，审计 subagent 基于 xxxx 圈定的边界做审计。

### 3. subagent 审计完成后：接管 + double check

审计 subagent 返回结果后，你必须继续接管，不能把结果直接转交给用户：

- **逐条 double check** 审计发现的每个问题：对照真实代码/证据核实（read_file、execute_command、git diff 等），区分"真实问题"与"误报"。
- **对确认为真实的问题**：给出修复方案并实施下一步改动。
- **对误报**：说明理由，不盲目采纳。
- **修复后重新验证**：确认问题已修复、无回归、改动仍只覆盖自己的范围，才向用户汇报完成。

## 约束

- 只处理与本次改动相关的问题；不要把别人的改动或历史遗留问题算进本次审计范围，也不要在汇报中把它们当作本次改动的产物。
- 不要因为审计 subagent 报了问题就直接照单全收，也不要因为它没报问题就草率收尾——double check 是本流程的必需环节。
- 不要把审计指令 xxxx 写成模糊的"帮我看看我的改动"，必须给出明确的文件清单和验证点，否则审计 subagent 无法区分你的改动与别人的改动。
