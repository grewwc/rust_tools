{
  "name": "spawn_process",
  "description": "Spawn a background process in the Agent OS. Returns the PID immediately, not the process's result — observe it via IPC (read_mailbox / wait_process) or side effects. Use task_spawn instead when you need the result returned.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Short name for the process."
      },
      "goal": {
        "type": "string",
        "description": "The specific goal or task for the sub-process to accomplish."
      },
      "priority": {
        "type": "integer",
        "description": "Process scheduling priority 0-255 (0=highest, 255=lowest). Default 10. Lower = more urgent; opposite direction of knowledge_save priority."
      },
      "quota_turns": {
        "type": "integer",
        "description": "Maximum tool-calling turns per scheduler run before the process yields. Default 10."
      },
      "capabilities": {
        "type": "object",
        "description": "Optional exact capability set for the child process. Omitted means inherit the parent capabilities.",
        "properties": {
          "spawn": {
            "type": "boolean",
            "description": "Allow spawning child processes."
          },
          "wait": {
            "type": "boolean",
            "description": "Allow waiting for child processes."
          },
          "ipc_send": {
            "type": "boolean",
            "description": "Allow sending IPC messages."
          },
          "ipc_receive": {
            "type": "boolean",
            "description": "Allow receiving IPC messages."
          },
          "env_write": {
            "type": "boolean",
            "description": "Allow modifying process-local environment variables."
          },
          "manage_children": {
            "type": "boolean",
            "description": "Allow managing child-process lifecycle."
          },
          "sleep": {
            "type": "boolean",
            "description": "Allow suspending the current process."
          },
          "reap": {
            "type": "boolean",
            "description": "Allow reaping terminated child processes."
          },
          "signal": {
            "type": "boolean",
            "description": "Allow signaling child or descendant processes."
          }
        }
      },
      "allowed_tools": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Additional tool names allowed alongside the baseline tool set. Omitted inherits the parent's allowlist; an empty array leaves tool access unrestricted."
      }
    },
    "required": [
      "name",
      "goal"
    ]
  },
  "groups": [
    "builtin",
    "executor"
  ],
  "hidden": true
}
