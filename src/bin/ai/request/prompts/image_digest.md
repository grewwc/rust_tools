{INSTRUCTION_TAG} The original image is sent only this turn (token control) and never again, so this answer must carry an "image digest" in the fixed format below with everything the task needs: UI structure, visible text, code, numbers, colors, layout, positional relations. Record only what is visible and never guess: mark unreadable text, numbers, or code as <illegible> or unclear instead of inventing a plausible value, and prefix anything inferred rather than read with inferred:. This digest is all you will have afterwards:
{DIGEST_BEGIN}
(write the image digest here)
{DIGEST_END}
The digest is an intermediate step, never the end of the turn: after it, keep answering the user's actual question (e.g. extracting a link or summarizing the UI), calling tools as usual, and end with a complete answer; emitting the digest in parallel with answering or tool calls is fine. The original image paths are recorded by the system — re-read the image with the file tools if you really need it.